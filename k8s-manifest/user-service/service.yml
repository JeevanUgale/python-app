apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: python
  labels:
    app: user-service
    environment: dev
spec:
  selector:
    app: user-service
    environment: dev
  ports:
    - port: 5002
      targetPort: 5002
  type: ClusterIP