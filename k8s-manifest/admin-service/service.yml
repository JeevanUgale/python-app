apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: python
  labels:
    app: admin-service
    environment: dev
spec:
  selector:
    app: admin-service
    environment: dev
  ports:
    - port: 5003
      targetPort: 5003
  type: ClusterIP