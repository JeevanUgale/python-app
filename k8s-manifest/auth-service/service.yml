apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: auth
  labels:
    app: auth-service
    environment: dev
spec:
  selector:
    app: auth-service
    environment: dev
  ports:
    - port: 5002
      targetPort: 5002
  type: ClusterIP