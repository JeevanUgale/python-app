apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: python
  labels:
    app: db-server
    environment: dev
spec:
  type: ExternalName
  externalName: 0.0.0.0
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP