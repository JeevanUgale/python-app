apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.auth.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "python-app.serviceLabels" (dict "service" "auth" "root" .) | nindent 4 }}
spec:
  selector:
    {{- include "python-app.serviceSelectorLabels" (dict "service" "auth" "root" .) | nindent 4 }}
  ports:
    - port: {{ .Values.services.auth.port }}
      targetPort: {{ .Values.services.auth.port }}
  type: ClusterIP
